{
  "name": "tdesign-miniprogram-starter",
  "version": "0.0.2",
  "main": "index.js",
  "scripts": {
    "pull": "git pull origin master",
    "push": "git add . && git commit -m 'x' && git push origin master",
    "release": "git add . && git commit -m 'x' && git push origin master && git push origin master:release"
  },
  "keywords": [
    "wechat",
    "miniprogram",
    "starter"
  ],
  "author": "leejim",
  "license": "MIT",
  "description": "a start-kit for wechat miniprogram powerby TDesign miniprogram",
  "dependencies": {
    "tdesign-miniprogram": "^1.8.6"
  },
  "lint-staged": {
    "{src,example,script}/**/*.{js,ts,wxml,html,json,less}": [
      "prettier --write"
    ],
    "**/*.{js,ts}": [
      "eslint --fix"
    ]
  }
}
